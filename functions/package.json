{
  "name": "deadline-shield-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build",
    "serve": "firebase emulators:start --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "cheerio": "^1.1.2",
    "firebase": "^12.7.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}